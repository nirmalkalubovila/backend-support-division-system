const authController = require('./auth/auth.controller');
const userController = require('./user-management/user.controller');
const projectController = require('./projects/project.controller');

module.exports = { authController, userController, projectController };
